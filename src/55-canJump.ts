/**
 * 55. 跳跃游戏 (Jump Game)
 * 
 * 给定一个非负整数数组 `nums`，其中 `nums[i]` 表示从索引 `i` 处可以跳跃的 **最大步数**。
 * 
 * 判断是否能够从数组的 **第 0 个索引** 位置跳到 **最后一个索引**。
 * 
 * **示例 1：**
 * 输入：nums = [2,3,1,1,4]
 * 输出：true
 * 解释：从索引 0 处，跳跃 2 步到索引 2，再跳跃 2 步到索引 4，成功到达终点。
 * 
 * **示例 2：**
 * 输入：nums = [3,2,1,0,4]
 * 输出：false
 * 解释：无论怎么跳跃，都无法到达索引 4，因为索引 3 处的 `nums[3] = 0` 无法再向前跳。
 * 
 * **提示：**
 * 1. `1 <= nums.length <= 10^4`
 * 2. `0 <= nums[i] <= 10^5`
 */

function canJump(nums: number[]): boolean {
    
};
